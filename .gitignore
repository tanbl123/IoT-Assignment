# ---- Secrets (never commit) ----
.env
*.env
secrets.h
serviceAccountKey.json
google-services.json
firebase_config.json
*.pem
*.key

# ---- Python ----
.venv/
venv/
env/
fall-detection-env/
*-env/
__pycache__/
*.pyc
*.pkl
*.joblib
data/*.csv
!data/README.md
!data/sample.csv

# ---- Datasets ----
# Raw UP-Fall / URFD downloads are many GB — NEVER commit these.
upfall_raw/
urfd_raw/
# NOTE: data/dataset.csv (the small generated 7-feature training file) IS
# committed on purpose, so teammates can run train_model.py without the multi-GB
# raw images. Add it with:  git add -f backend-python/data/dataset.csv

# ---- Arduino / PlatformIO ----
.pio/
build/
*.hex
*.bin

# ---- Android / Gradle ----
android-app-kotlin/.gradle/
android-app-kotlin/build/
android-app-kotlin/app/build/
android-app-kotlin/local.properties
*.iml
.idea/

# ---- OS ----
.DS_Store
Thumbs.db
